import 'package:flutter/material.dart';

class SignupPage extends StatelessWidget{
  Widget build (BuildContext context){
    return new Scaffold(
        body: Image.asset('assets/logo.png', scale: 10),
      );
  }
}